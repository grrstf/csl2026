# frozen_string_literal: true

source "https://rubygems.org"

# git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# Added since not default installed in Ruby 3.0
# gem "webrick"

gem "jekyll"

group :jekyll_plugins do
    gem "jekyll-feed"
    gem "jekyll-sitemap"
    gem "jekyll-seo-tag"
    gem 'jekyll-email-protect'
  end
